# Server
PORT=3001
HOST=0.0.0.0

# Comma-separated origins allowed by CORS (Vite dev server default)
CORS_ORIGIN=http://localhost:5173

# Loyverse (add when ready — never commit real values)
LOYVERSE_ACCESS_TOKEN=
LOYVERSE_API_BASE_URL=https://api.loyverse.com/v1.0
